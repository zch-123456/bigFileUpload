# 📖 安装指南

## 快速开始（推荐）

### 方式一：使用启动脚本（Windows）

双击运行 `快速启动.bat` 文件，脚本会自动：
1. 检查 pnpm 是否安装
2. 安装所有依赖
3. 启动前后端服务

### 方式二：手动安装

#### 1️⃣ 确保已安装必要工具

```powershell
# 检查 Node.js 版本（需要 >= 16）
node -v

# 检查 pnpm 是否安装
pnpm -v

# 如果未安装 pnpm，执行以下命令安装
npm install -g pnpm
```

#### 2️⃣ 安装项目依赖

```powershell
# 在项目根目录执行

# 方法1：安装所有依赖
pnpm install

# 方法2：分别安装
# 安装根目录依赖
pnpm install

# 安装前端依赖
cd frontend
pnpm install
cd ..

# 安装后端依赖
cd backend
pnpm install
cd ..
```

#### 3️⃣ 启动项目

##### 同时启动前后端（推荐）

```powershell
pnpm dev
```

##### 分别启动前后端

```powershell
# 打开第一个终端 - 启动后端
cd backend
pnpm dev

# 打开第二个终端 - 启动前端
cd frontend
pnpm dev
```

#### 4️⃣ 访问应用

- 前端：http://localhost:5173
- 后端：http://localhost:3000

## 🔍 验证安装

### 1. 检查后端服务

在浏览器访问：http://localhost:3000/health

应该看到：
```json
{
  "status": "ok",
  "message": "Server is running"
}
```

### 2. 检查前端页面

访问：http://localhost:5173

应该看到"大文件分片上传系统"页面

### 3. 测试上传功能

1. 选择一个文件（建议先用小文件测试，如 1-10MB）
2. 点击"开始上传"
3. 观察上传进度
4. 上传完成后查看下载链接

## ⚠️ 常见问题

### 问题1：pnpm 命令不可用

**解决方案：**
```powershell
# 全局安装 pnpm
npm install -g pnpm

# 或使用 npm 代替
npm install
npm run dev
```

### 问题2：端口被占用

**症状：** 启动时提示端口 3000 或 5173 被占用

**解决方案：**
```powershell
# 方法1：修改端口
# 修改 backend/src/app.js 中的 PORT
# 修改 frontend/vite.config.js 中的 port

# 方法2：杀死占用端口的进程
# 查找占用端口的进程
netstat -ano | findstr :3000
# 杀死进程（PID 为上面查到的进程号）
taskkill /PID <进程号> /F
```

### 问题3：依赖安装失败

**解决方案：**
```powershell
# 清理缓存
pnpm store prune

# 删除 node_modules 和 lock 文件
Remove-Item -Recurse -Force node_modules, frontend/node_modules, backend/node_modules
Remove-Item pnpm-lock.yaml

# 重新安装
pnpm install
```

### 问题4：PowerShell 脚本无法执行

**症状：** 运行 start.ps1 提示"无法加载..."

**解决方案：**
```powershell
# 临时允许脚本执行
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope Process

# 或永久允许（需要管理员权限）
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

### 问题5：上传失败

**排查步骤：**
1. 检查后端服务是否正常运行
2. 打开浏览器开发者工具查看网络请求
3. 查看后端控制台日志
4. 检查磁盘空间是否充足

## 📦 生产环境部署

### 构建前端

```powershell
cd frontend
pnpm build
```

构建产物在 `frontend/dist` 目录

### 部署后端

```powershell
cd backend

# 使用 pm2 管理进程
npm install -g pm2
pm2 start src/app.js --name "upload-server"

# 或使用 node 直接运行
node src/app.js
```

### Nginx 配置示例

```nginx
server {
    listen 80;
    server_name your-domain.com;

    # 前端静态文件
    location / {
        root /path/to/frontend/dist;
        try_files $uri $uri/ /index.html;
    }

    # API 代理
    location /api {
        proxy_pass http://localhost:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        
        # 大文件上传配置
        client_max_body_size 1000M;
        proxy_request_buffering off;
    }

    # 文件下载
    location /files {
        proxy_pass http://localhost:3000;
    }
}
```

## 🎯 下一步

1. 尝试上传不同大小的文件
2. 测试断点续传功能（上传过程中刷新页面）
3. 查看任务列表
4. 根据需要调整配置参数

## 💡 提示

- 建议先用小文件（< 100MB）测试功能
- 上传大文件时请确保磁盘空间充足
- 开发环境下数据库文件在 `backend/data/upload.db`
- 上传的文件在 `backend/uploads/merged/` 目录

## 🆘 获取帮助

如遇到问题：
1. 查看 README.md
2. 检查浏览器控制台和后端日志
3. 提交 Issue（如果是 bug）

